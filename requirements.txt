# No third-party Python packages are required to build or preview this
# site. index.qmd has no code cells (no Jupyter/Python engine involved),
# and assets/curriculum/make.py only uses the standard library.
#
# What you actually need is installed outside of pip/conda:
#   - Quarto CLI            https://quarto.org/docs/get-started/
#   - pdflatex (TeX Live)   only if rebuilding assets/curriculum/*.pdf
#   - ImageMagick `convert` only if rerunning assets/img/crop.sh
